import React, {useEffect, useState} from 'react';

const AddProducts = (props) => {
    let [product, setProducts] = useState({productId: "", productName:"", productPrice: "", productImage: "", shopId: ""})

    return (
        <div>

        </div>
    );
};

export default AddProducts;